from django.db import models

### Add antenna circularity to drone antenna build
# LEFT_CIRCULAR = 'left_circular', 'Left-hand Circular, LHCP'
# RIGHT_CIRCULAR = 'right_circular', 'Right-hand Circular, RHCP'

# class Drone(models.Model):
#     frame = models.ForeignKey('components.Frame', on_delete=models.SET_NULL, null=True)
